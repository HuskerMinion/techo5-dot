# Upstream: 75bbd2ea50ba1c5d9da878a17e92eac02fe0fd3a "Bluetooth: Prevent out-of-bounds read in hci_inquiry_result_evt()"
# Taken from: linux-4.4.y stable backport a3cc6834b0e2c39c9e8d84f67745c501b33867b1
# CVE: none assigned
# 3.18 adaptation: Context only: 3.18 tests HCI_PERIODIC_INQ with test_bit() instead of hci_dev_test_flag(). Code change identical.
#
diff --git a/net/bluetooth/hci_event.c b/net/bluetooth/hci_event.c
--- a/net/bluetooth/hci_event.c
+++ b/net/bluetooth/hci_event.c
@@ -1997,7 +1997,7 @@ static void hci_inquiry_result_evt(struct hci_dev *hdev, struct sk_buff *skb)
 
 	BT_DBG("%s num_rsp %d", hdev->name, num_rsp);
 
-	if (!num_rsp)
+	if (!num_rsp || skb->len < num_rsp * sizeof(*info) + 1)
 		return;
 
 	if (test_bit(HCI_PERIODIC_INQ, &hdev->dev_flags))
